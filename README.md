💊 İlaç/Vitamin Görüntü Sınıflandırma ProjesiBu proje, görüntü işleme ve derin öğrenme (Transfer Öğrenimi) kullanarak ilaç ve vitaminleri bir görüntüden otomatik olarak sınıflandıran bir Web API ve basit bir ön uç (frontend) uygulamasıdır. MobileNetV2 önceden eğitilmiş modeli kullanılarak 10 farklı ilaç/vitamin türü yüksek doğrulukla tespit edilmektedir.✨ Temel TeknolojilerBu proje, modern ve güçlü teknolojilerin bir kombinasyonu kullanılarak geliştirilmiştir:AlanTeknolojiAçıklamaDerin ÖğrenmeTensorFlow, KerasModelin oluşturulması, eğitilmesi ve ağırlıkların kaydedilmesi.Transfer ÖğrenimiMobileNetV2Görüntü özelliklerini çıkarmak için kullanılan önceden eğitilmiş hafif model.API GeliştirmeFastAPIYüksek performanslı ve modern Python API sunucusu. Tahmin (Prediction) uç noktasını barındırır.KonteynerleştirmeDocker, Docker ComposeTüm uygulamanın (API + Ön Uç) tutarlı ve taşınabilir bir ortamda çalıştırılması.Ön Uç (Frontend)React, ViteKullanıcının resim yükleyip tahmin sonucunu görebileceği basit kullanıcı arayüzü.📁 Veri SetiBu projenin eğitildiği veri seti Kaggle platformundan alınmıştır. Projenin yerel olarak yeniden eğitilmesi için veri setine ve orijinal dosyalarınıza erişim gereklidir.Kaggle Veri Seti Linki:https://www.kaggle.com/datasets/vencerlanz09/pharmaceutical-drugs-and-vitamins-synthetic-images  
Açıklama: drug_cnn.py dosyası, Data Combined klasörü altındaki bu veri setini kullanarak MobileNetV2 modelini eğitir ve ağırlıkları (checkpoint.weights.h5) kaydeder.🚀 Kurulum ve ÇalıştırmaProjenin tamamını çalıştırmak için Docker ve Docker Compose kurulu olmalıdır. Bu yöntem, hem Python API'sinin hem de React ön yüzünün kolayca kurulmasını ve çalıştırılmasını sağlar.1. Proje YapısıAşağıdaki dosya ve klasörlerin kök dizinde (root) bulunduğundan emin olun:.
├── main.py             # FastAPI uygulaması ve tahmin API'si
├── drug_cnn.py         # Keras/TensorFlow model eğitim betiği (eğitim için kullanılır)
├── checkpoint.weights.h5 # Eğitilmiş model ağırlıkları (API için hayati)
├── requirements.txt    # Python bağımlılıkları
├── Dockerfile          # Docker imajı oluşturma talimatları
├── docker-compose.yml  # Servisleri yönetme dosyası
└── UI/                 # React ön yüz klasörü
    └── vitaminui/      # (package.json, App.jsx, vb. içerir)
2. Bağımlılıkları Yükleme ve İmaj OluşturmaProjenin kök dizininde (root directory) iken aşağıdaki komutu çalıştırın:Bashdocker-compose up --build -d
--build: Dockerfile'ı kullanarak yeni imajı oluşturur. Bu, hem Python bağımlılıklarını kuracak hem de React ön yüzünü (UI) derleyecektir.-d: Servisi arka planda (detached mode) çalıştırır.3. Uygulamaya ErişmeKonteyner başarıyla başlatıldıktan sonra, uygulamaya tarayıcınızdan aşağıdaki adresten erişebilirsiniz:http://localhost:80
( docker-compose.yml dosyasında 8000 portunu 80'e yönlendirdiğiniz için)⚙️ Uygulama Bileşenlerinin Detaylarımain.py (FastAPI API)Model Yükleme: Uygulama başlarken (@app.on_event("startup")), build_model() fonksiyonu ile MobileNetV2 mimarisi yeniden oluşturulur ve kaydedilen checkpoint.weights.h5 ağırlıkları yüklenir.Tahmin Uç Noktası (/api/predict): Yüklenen resim dosyası (UploadFile) alınır, 224x224 boyutuna yeniden boyutlandırılır, MobileNetV2 için ön işlenir ve model ile tahmin yapılır. En yüksek olasılığa sahip sınıf ve tüm olasılıklar JSON olarak döndürülür.Ön Yüz Sunumu: / uç noktası, Dockerfile'da derlenip /app/static altına kopyalanan React uygulamasının ana dosyası olan static/index.html dosyasını sunar.drug_cnn.py (Model Eğitimi)Bu dosya, modeli eğitmek için kullanılan betiktir. API'yi çalıştırmak için gerekli değildir, ancak modelin nasıl eğitildiğini gösterir.Transfer Öğrenimi: MobileNetV2 modelinin include_top=False parametresi ile sınıflandırma katmanı çıkarılır. Temel katmanlar trainable = False ile dondurulur.Özel Sınıflandırma Bloğu: Dondurulmuş MobileNetV2'nin çıktısına Dropout ile birlikte iki adet Dense (256, relu) katmanı ve son olarak 10 sınıflı Softmax çıktı katmanı eklenir.Callback'ler: ModelCheckpoint (en iyi ağırlıkları kaydetmek için) ve EarlyStopping (aşırı öğrenmeyi önlemek için) callback'leri kullanılır.Optimizasyon: Adam optimizasyonu ve düşük bir öğrenme hızı (0.0001) ile categorical_crossentropy kayıp fonksiyonu kullanılarak derlenir.DockerfileÇok Aşamalı Yapı (Multi-stage build):frontend-builder (Node:20-alpine): React ön yüzünü derler ve statik dosyaları dist klasörüne hazırlar.final-stage (Python:3.11-slim): Python ortamını kurar, bağımlılıkları yükler, kaynak kodları ve checkpoint.weights.h5 dosyasını ekler.COPY --from=frontend-builder: Derlenmiş ön yüz dosyalarını API'nin sunabileceği /app/static klasörüne kopyalar.CMD: Uygulamayı uvicorn ile 8000 portunda başlatır.docker-compose.ymlapp servisi: Proje kök dizinindeki Dockerfile'ı kullanarak imajı oluşturur.Port Eşleştirme: Konteynerin içindeki 8000 portunu ana makinenin 80 portuna ("80:8000") yönlendirir.
